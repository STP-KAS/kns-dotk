# For @supertypo

**This repository is not an audit.**

It is a desk sketch: keep KNS `.kas` as the human string, keep DOTK’s gap-covering *idea* as uniqueness, **bind don’t alias**. It is not your product. It does not take lineage `ee2128c0…`. It does not say `.k` should die. It does not say KNS should ship your bytecode.

You are added as a **collaborator (push)** so you can check that we did not mis-describe:

- gap covering / hash-blind split / cards / self-proving deed addresses
- why `.kas` and `.k` must not auto-map (`supertypo.kas` ≠ `supertypo.k` on 15 Sep 2026)

Independent DOTK pass (also inviting you, also **not an audit**): https://github.com/STP-KAS/dotk-review

Please comment, open an issue, or PR.
